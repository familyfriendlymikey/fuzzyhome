{
  "name": "fuzzyhome",
  "version": "3.1.0",
  "description": "A lightweight new-tab page that lets you very quickly fuzzy find links and navigate to a result.",
  "homepage": "https://fuzzyho.me/",
  "author": "Mikey Oz (https://github.com/familyfriendlymikey)",
  "scripts": {
    "dev": "imba build --esm --force -w index.html -d -o chrome/dist -M --base .",
    "build": "rm -rf chrome/dist && imba build --esm --force index.html -o chrome/dist -M --base . && zip -r chrome.zip chrome"
  },
  "dependencies": {
    "fzi": "^1.5.0",
    "imba": "^2.0.0-alpha.237",
    "lodash": "^4.17.21",
    "math-expression-evaluator": "^2.0.0",
    "vite": "^3.2.5",
    "vite-plugin-imba": "^0.10.1"
  }
}
